<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>StudyMind — Aprende jugando</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Syne:wght@400;600;700;800&family=DM+Sans:ital,wght@0,300;0,400;0,500;1,300&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #0a0a0f;
    --surface: #13131a;
    --card: #1a1a24;
    --border: #2a2a3d;
    --accent: #7c5cfc;
    --accent2: #fc5c7d;
    --accent3: #5cf7c4;
    --accent4: #fcb45c;
    --text: #f0eeff;
    --muted: #7a7a9a;
    --radius: 16px;
  }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body { background: var(--bg); color: var(--text); font-family: 'DM Sans', sans-serif; min-height: 100vh; overflow-x: hidden; }

  body::before {
    content: '';
    position: fixed; inset: 0;
    background:
      radial-gradient(ellipse 60% 40% at 20% 10%, rgba(124,92,252,0.12) 0%, transparent 60%),
      radial-gradient(ellipse 40% 30% at 80% 80%, rgba(252,92,125,0.08) 0%, transparent 50%),
      radial-gradient(ellipse 50% 50% at 50% 50%, rgba(92,247,196,0.04) 0%, transparent 70%);
    pointer-events: none; z-index: 0;
  }
  .app { position: relative; z-index: 1; }

  /* ── HEADER ── */
  header {
    display: flex; align-items: center; justify-content: space-between;
    padding: 22px 40px;
    border-bottom: 1px solid var(--border);
    backdrop-filter: blur(20px);
    position: sticky; top: 0; z-index: 100;
    background: rgba(10,10,15,0.85);
  }
  .logo { font-family: 'Syne', sans-serif; font-weight: 800; font-size: 1.4rem; background: linear-gradient(135deg, var(--accent), var(--accent2)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; letter-spacing: -0.02em; }
  .logo span { -webkit-text-fill-color: var(--accent3); }
  .status-pill { display: flex; align-items: center; gap: 8px; padding: 7px 16px; border: 1px solid var(--border); border-radius: 100px; font-size: 0.78rem; color: var(--muted); }
  .status-dot { width: 7px; height: 7px; border-radius: 50%; background: var(--accent3); animation: blink 2s infinite; }
  @keyframes blink { 0%,100%{opacity:1} 50%{opacity:0.3} }

  /* ── MAIN ── */
  main { max-width: 980px; margin: 0 auto; padding: 56px 24px 80px; }

  /* ── HERO ── */
  .hero { text-align: center; margin-bottom: 56px; }
  .hero-tag { display: inline-flex; align-items: center; gap: 8px; padding: 6px 16px; border: 1px solid rgba(124,92,252,0.4); border-radius: 100px; font-size: 0.76rem; color: var(--accent); letter-spacing: 0.08em; text-transform: uppercase; margin-bottom: 22px; background: rgba(124,92,252,0.07); }
  h1 { font-family: 'Syne', sans-serif; font-weight: 800; font-size: clamp(2rem,5vw,3.2rem); line-height: 1.1; letter-spacing: -0.03em; margin-bottom: 16px; }
  h1 .hl { background: linear-gradient(135deg, var(--accent), var(--accent2)); -webkit-background-clip: text; -webkit-text-fill-color: transparent; }
  .hero-sub { color: var(--muted); font-size: 1rem; max-width: 500px; margin: 0 auto; line-height: 1.65; font-weight: 300; }

  /* ── UPLOAD ── */
  .drop-zone { border: 2px dashed var(--border); border-radius: var(--radius); padding: 52px 32px; text-align: center; cursor: pointer; transition: all .3s; background: rgba(255,255,255,0.01); position: relative; overflow: hidden; }
  .drop-zone::before { content:''; position:absolute; inset:0; background:radial-gradient(ellipse at center,rgba(124,92,252,0.06) 0%,transparent 70%); opacity:0; transition:opacity .3s; }
  .drop-zone:hover, .drop-zone.dragover { border-color: var(--accent); background: rgba(124,92,252,0.03); }
  .drop-zone:hover::before, .drop-zone.dragover::before { opacity:1; }
  .drop-icon { font-size:2.8rem; margin-bottom:14px; display:block; }
  .drop-title { font-family:'Syne',sans-serif; font-weight:700; font-size:1.15rem; margin-bottom:6px; }
  .drop-sub { color:var(--muted); font-size:0.85rem; }
  .drop-btn { display:inline-block; margin-top:18px; padding:11px 26px; background:linear-gradient(135deg,var(--accent),#9b7dff); border-radius:100px; color:white; font-weight:500; font-size:0.88rem; border:none; cursor:pointer; transition:all .2s; font-family:'DM Sans',sans-serif; }
  .drop-btn:hover { transform:translateY(-2px); box-shadow:0 8px 24px rgba(124,92,252,0.3); }
  #fileInput { display:none; }

  .file-preview { display:none; align-items:center; gap:16px; padding:18px 22px; background:var(--card); border:1px solid var(--border); border-radius:var(--radius); margin-top:14px; }
  .file-preview.show { display:flex; }
  .file-icon-box { width:46px;height:46px; border-radius:12px; background:linear-gradient(135deg,var(--accent),var(--accent2)); display:flex;align-items:center;justify-content:center; font-size:1.3rem; flex-shrink:0; }
  .file-info { flex:1; }
  .file-name { font-weight:500; font-size:0.92rem; margin-bottom:2px; }
  .file-size { color:var(--muted); font-size:0.8rem; }
  .remove-file { background:none;border:none;color:var(--muted);cursor:pointer;font-size:1.1rem;padding:8px;transition:color .2s; }
  .remove-file:hover { color:var(--accent2); }

  .analyze-btn { width:100%; padding:17px; background:linear-gradient(135deg,var(--accent),var(--accent2)); border:none; border-radius:var(--radius); color:white; font-family:'Syne',sans-serif; font-weight:700; font-size:1.05rem; cursor:pointer; transition:all .3s; letter-spacing:.02em; display:none; position:relative; overflow:hidden; margin-top:14px; }
  .analyze-btn::after { content:''; position:absolute; inset:0; background:linear-gradient(135deg,transparent,rgba(255,255,255,0.08),transparent); transform:translateX(-100%); transition:transform .5s; }
  .analyze-btn:hover::after { transform:translateX(100%); }
  .analyze-btn:hover { transform:translateY(-2px); box-shadow:0 12px 32px rgba(124,92,252,0.35); }
  .analyze-btn.show { display:block; }
  .analyze-btn:disabled { opacity:.6;cursor:not-allowed;transform:none; }

  /* ── LOADING ── */
  .loading-section { display:none; text-align:center; padding:64px 32px; }
  .loading-section.show { display:block; }
  .loader-ring { width:60px;height:60px;border:3px solid var(--border);border-top-color:var(--accent);border-radius:50%;animation:spin 1s linear infinite;margin:0 auto 22px; }
  @keyframes spin { to{transform:rotate(360deg)} }
  .loading-text { font-family:'Syne',sans-serif;font-weight:700;font-size:1.15rem;margin-bottom:6px; }
  .loading-sub { color:var(--muted);font-size:0.85rem;margin-bottom:28px; }
  .loading-steps { display:flex;justify-content:center;gap:8px;flex-wrap:wrap; }
  .step-chip { padding:6px 14px;border-radius:100px;font-size:0.76rem;border:1px solid var(--border);color:var(--muted);transition:all .4s; }
  .step-chip.active { border-color:var(--accent);color:var(--accent);background:rgba(124,92,252,0.1); }
  .step-chip.done { border-color:var(--accent3);color:var(--accent3);background:rgba(92,247,196,0.07); }

  /* ── RESULTS ── */
  .results-section { display:none; }
  .results-section.show { display:block; }

  .summary-card { background:var(--card);border:1px solid var(--border);border-radius:var(--radius);padding:30px;margin-bottom:36px;position:relative;overflow:hidden; }
  .summary-card::before { content:'';position:absolute;top:0;left:0;right:0;height:3px;background:linear-gradient(90deg,var(--accent),var(--accent2),var(--accent3)); }
  .subject-badge { display:inline-flex;align-items:center;gap:8px;padding:6px 16px;border-radius:100px;font-size:0.78rem;font-weight:600;margin-bottom:18px;text-transform:uppercase;letter-spacing:.06em; }
  .summary-title { font-family:'Syne',sans-serif;font-size:1.25rem;font-weight:700;margin-bottom:8px; }
  .summary-desc { color:var(--muted);font-size:0.88rem;line-height:1.6;margin-bottom:20px; }
  .summary-grid { display:grid;grid-template-columns:1fr 1fr;gap:24px; }
  @media(max-width:600px){.summary-grid{grid-template-columns:1fr}}
  .summary-block h4 { font-family:'Syne',sans-serif;font-size:0.74rem;font-weight:700;text-transform:uppercase;letter-spacing:.1em;color:var(--muted);margin-bottom:10px; }
  .idea-tag { display:inline-block;padding:5px 12px;background:rgba(124,92,252,0.09);border:1px solid rgba(124,92,252,0.2);border-radius:100px;font-size:0.8rem;color:var(--accent);margin:3px; }
  .key-point { display:flex;align-items:flex-start;gap:10px;margin-bottom:9px;font-size:0.87rem;line-height:1.5;color:#c8c8e8; }
  .key-point::before { content:'▸';color:var(--accent3);flex-shrink:0;margin-top:1px; }

  /* ── GAMES GRID ── */
  .games-header { display:flex;align-items:center;justify-content:space-between;margin-bottom:24px;flex-wrap:wrap;gap:12px; }
  .games-title { font-family:'Syne',sans-serif;font-weight:800;font-size:1.5rem;letter-spacing:-.02em; }
  .games-count { color:var(--muted);font-size:0.82rem;padding:6px 14px;border:1px solid var(--border);border-radius:100px; }
  .games-grid { display:grid;grid-template-columns:repeat(auto-fill,minmax(270px,1fr));gap:18px; }

  .game-card { background:var(--card);border:1px solid var(--border);border-radius:var(--radius);padding:26px;cursor:pointer;transition:all .25s;position:relative;overflow:hidden; }
  .game-card::after { content:'';position:absolute;bottom:0;left:0;right:0;height:2px;background:var(--gc,var(--accent));transform:scaleX(0);transition:transform .3s; }
  .game-card:hover { border-color:rgba(255,255,255,0.11);transform:translateY(-3px);box-shadow:0 12px 32px rgba(0,0,0,0.3); }
  .game-card:hover::after { transform:scaleX(1); }
  .game-emoji { font-size:2.2rem;margin-bottom:12px;display:block; }
  .game-name { font-family:'Syne',sans-serif;font-weight:700;font-size:1rem;margin-bottom:5px; }
  .game-desc { color:var(--muted);font-size:0.82rem;line-height:1.5;margin-bottom:14px; }
  .game-tag { display:inline-block;padding:4px 10px;border-radius:100px;font-size:0.73rem;font-weight:600;background:rgba(255,255,255,0.05);color:var(--muted); }
  .play-btn { display:flex;align-items:center;justify-content:center;gap:8px;width:100%;padding:11px;background:rgba(255,255,255,0.04);border:1px solid var(--border);border-radius:10px;color:var(--text);font-family:'DM Sans',sans-serif;font-size:0.85rem;font-weight:500;cursor:pointer;transition:all .2s;margin-top:14px; }
  .play-btn:hover { background:rgba(124,92,252,0.1);border-color:var(--accent);color:var(--accent); }

  /* ── MODAL ── */
  .game-modal { display:none;position:fixed;inset:0;z-index:200;background:rgba(0,0,0,0.82);backdrop-filter:blur(10px);overflow-y:auto; }
  .game-modal.show { display:flex;align-items:flex-start;justify-content:center;padding:28px 16px; }
  .gm-inner { background:var(--surface);border:1px solid var(--border);border-radius:20px;width:100%;max-width:800px;min-height:500px;position:relative;overflow:hidden; }
  .gm-header { display:flex;align-items:center;justify-content:space-between;padding:22px 30px;border-bottom:1px solid var(--border); }
  .gm-title { font-family:'Syne',sans-serif;font-weight:700;font-size:1.05rem; }
  .gm-sub { color:var(--muted);font-size:0.8rem;margin-top:2px; }
  .close-btn { background:rgba(255,255,255,0.05);border:1px solid var(--border);color:var(--text);width:34px;height:34px;border-radius:50%;cursor:pointer;font-size:0.95rem;transition:all .2s;display:flex;align-items:center;justify-content:center; }
  .close-btn:hover { background:rgba(252,92,125,0.12);border-color:var(--accent2);color:var(--accent2); }
  .gm-body { padding:28px 30px; }

  /* ── GAME COMPONENTS ── */
  .progress-bar { height:3px;background:var(--border);border-radius:100px;margin-bottom:24px;overflow:hidden; }
  .progress-fill { height:100%;background:linear-gradient(90deg,var(--accent),var(--accent2));border-radius:100px;transition:width .4s ease; }
  .score-row { display:flex;align-items:center;justify-content:space-between;padding:11px 18px;background:var(--card);border-radius:12px;margin-bottom:20px;border:1px solid var(--border); }
  .score-label { color:var(--muted);font-size:0.82rem; }
  .score-val { font-family:'Syne',sans-serif;font-weight:700;font-size:1.05rem;color:var(--accent3); }

  /* Flashcards */
  .flashcard-wrap { perspective:1000px;text-align:center; }
  .flashcard { width:100%;max-width:460px;height:230px;margin:0 auto 28px;cursor:pointer;position:relative;transform-style:preserve-3d;transition:transform .6s cubic-bezier(.4,0,.2,1); }
  .flashcard.flipped { transform:rotateY(180deg); }
  .card-face { position:absolute;inset:0;border-radius:14px;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:28px;backface-visibility:hidden;-webkit-backface-visibility:hidden; }
  .card-front { background:linear-gradient(135deg,rgba(124,92,252,.14),rgba(124,92,252,.04));border:1px solid rgba(124,92,252,.3); }
  .card-back { background:linear-gradient(135deg,rgba(92,247,196,.11),rgba(92,247,196,.03));border:1px solid rgba(92,247,196,.3);transform:rotateY(180deg); }
  .card-label { font-size:0.7rem;font-weight:700;text-transform:uppercase;letter-spacing:.1em;color:var(--muted);margin-bottom:14px; }
  .card-content { font-family:'Syne',sans-serif;font-size:1.1rem;font-weight:600;text-align:center;line-height:1.4; }
  .card-hint { font-size:0.75rem;color:var(--muted);margin-top:14px; }
  .card-nav { display:flex;align-items:center;justify-content:center;gap:14px; }
  .nav-btn { padding:9px 22px;border-radius:100px;border:1px solid var(--border);background:rgba(255,255,255,0.04);color:var(--text);cursor:pointer;font-family:'DM Sans',sans-serif;font-size:0.85rem;transition:all .2s; }
  .nav-btn:hover { border-color:var(--accent);color:var(--accent);background:rgba(124,92,252,0.08); }
  .nav-btn:disabled { opacity:.3;cursor:not-allowed; }
  .card-counter { color:var(--muted);font-size:0.82rem;min-width:56px;text-align:center; }

  /* Ordering */
  .timeline-list { list-style:none;margin-bottom:24px; }
  .timeline-item { display:flex;align-items:center;gap:14px;padding:14px 18px;background:var(--card);border:1px solid var(--border);border-radius:12px;margin-bottom:8px;cursor:grab;transition:all .2s;user-select:none; }
  .timeline-item:hover { border-color:rgba(124,92,252,.4);transform:translateX(3px); }
  .timeline-item.dragging { opacity:.45;border-color:var(--accent2); }
  .timeline-item.correct { border-color:var(--accent3);background:rgba(92,247,196,.05); }
  .timeline-item.wrong { border-color:var(--accent2);background:rgba(252,92,125,.05); }
  .drag-handle { color:var(--muted);font-size:1.1rem;cursor:grab; }
  .timeline-num { width:30px;height:30px;border-radius:50%;background:rgba(124,92,252,.12);border:1px solid rgba(124,92,252,.3);display:flex;align-items:center;justify-content:center;font-family:'Syne',sans-serif;font-weight:700;font-size:0.82rem;color:var(--accent);flex-shrink:0; }

  /* Fill blank */
  .blank-sentence { font-size:1.15rem;line-height:2.1;margin-bottom:20px;font-family:'Syne',sans-serif;font-weight:600; }
  .blank-input { display:inline-block;width:130px;padding:3px 10px;background:rgba(124,92,252,.09);border:none;border-bottom:2px solid var(--accent);border-radius:4px;color:var(--text);font-family:'Syne',sans-serif;font-weight:600;font-size:1.05rem;text-align:center;outline:none;transition:all .2s; }
  .blank-input:focus { background:rgba(124,92,252,.14);border-bottom-color:var(--accent2); }
  .blank-input.correct { border-bottom-color:var(--accent3);background:rgba(92,247,196,.09); }
  .blank-input.wrong { border-bottom-color:var(--accent2);background:rgba(252,92,125,.07);animation:shake .3s; }
  @keyframes shake { 0%,100%{transform:translateX(0)} 25%{transform:translateX(-6px)} 75%{transform:translateX(6px)} }

  /* Word sort */
  .sentence-builder { min-height:56px;border:2px dashed var(--border);border-radius:12px;padding:14px;display:flex;flex-wrap:wrap;gap:7px;align-items:center;margin-bottom:18px;background:rgba(255,255,255,.01);transition:border-color .2s; }
  .sentence-builder.active { border-color:var(--accent);border-style:solid; }
  .word-bank { display:flex;flex-wrap:wrap;gap:7px; }
  .word-chip { padding:7px 15px;border-radius:100px;background:var(--card);border:1px solid var(--border);cursor:pointer;font-size:0.88rem;transition:all .2s;user-select:none; }
  .word-chip:hover { border-color:var(--accent);color:var(--accent);transform:translateY(-2px); }
  .word-chip.placed { opacity:.28;cursor:not-allowed;pointer-events:none; }
  .word-chip.in-builder { background:rgba(124,92,252,.1);border-color:var(--accent);color:var(--accent); }

  /* Memory */
  .memory-grid { display:grid;grid-template-columns:repeat(4,1fr);gap:9px;max-width:460px;margin:0 auto 20px; }
  @media(max-width:460px){ .memory-grid{grid-template-columns:repeat(3,1fr)} }
  .mem-card { aspect-ratio:1;cursor:pointer;perspective:400px; }
  .mem-card-inner { width:100%;height:100%;position:relative;transform-style:preserve-3d;transition:transform .4s cubic-bezier(.4,0,.2,1);border-radius:10px; }
  .mem-card.flipped .mem-card-inner { transform:rotateY(180deg); }
  .mem-face { position:absolute;inset:0;border-radius:10px;display:flex;align-items:center;justify-content:center;backface-visibility:hidden;-webkit-backface-visibility:hidden;padding:7px; }
  .mem-back { background:linear-gradient(135deg,rgba(124,92,252,.18),rgba(124,92,252,.06));border:1px solid rgba(124,92,252,.3);font-size:1.3rem; }
  .mem-front { background:var(--card);border:1px solid var(--border);font-size:0.68rem;font-weight:500;text-align:center;color:var(--text);transform:rotateY(180deg);line-height:1.3; }
  .mem-card.matched .mem-face { border-color:var(--accent3)!important;background:rgba(92,247,196,.07)!important; }

  /* Concept map */
  .concept-map { position:relative;height:360px;background:var(--card);border-radius:14px;border:1px solid var(--border);overflow:hidden;margin-bottom:20px; }
  .concept-map svg { width:100%;height:100%; }

  /* Shared buttons */
  .check-btn { padding:13px 30px;background:linear-gradient(135deg,var(--accent3),#3dd6a0);border:none;border-radius:100px;color:#0a0f0e;font-family:'Syne',sans-serif;font-weight:700;font-size:0.92rem;cursor:pointer;transition:all .2s; }
  .check-btn:hover { transform:translateY(-2px);box-shadow:0 8px 24px rgba(92,247,196,.22); }
  .next-btn { padding:11px 26px;background:rgba(255,255,255,.05);border:1px solid var(--border);border-radius:100px;color:var(--text);font-family:'DM Sans',sans-serif;cursor:pointer;transition:all .2s;margin-left:10px; }
  .next-btn:hover { border-color:var(--accent);color:var(--accent); }
  .reset-btn { padding:9px 18px;background:none;border:1px solid var(--border);border-radius:100px;color:var(--muted);cursor:pointer;font-size:0.82rem;transition:all .2s;font-family:'DM Sans',sans-serif; }
  .reset-btn:hover { border-color:var(--accent2);color:var(--accent2); }
  .btn-row { display:flex;align-items:center;gap:10px;margin-top:20px;flex-wrap:wrap; }

  /* Feedback */
  .feedback-msg { padding:12px 18px;border-radius:12px;font-weight:500;margin-top:14px;font-size:0.87rem;display:none; }
  .feedback-msg.show { display:block; }
  .feedback-msg.correct { background:rgba(92,247,196,.09);border:1px solid rgba(92,247,196,.28);color:var(--accent3); }
  .feedback-msg.wrong { background:rgba(252,92,125,.07);border:1px solid rgba(252,92,125,.2);color:var(--accent2); }

  /* Completion screen */
  .complete-screen { text-align:center;padding:48px 0; }
  .complete-icon { font-size:3rem;margin-bottom:14px; }
  .complete-title { font-family:'Syne',sans-serif;font-size:1.35rem;font-weight:700;margin-bottom:8px; }
  .complete-sub { color:var(--muted);margin-bottom:24px;font-size:0.9rem; }

  /* Error banner */
  .error-banner { background:rgba(252,92,125,.08);border:1px solid rgba(252,92,125,.25);border-radius:12px;padding:16px 20px;color:var(--accent2);font-size:0.88rem;margin-top:16px;display:none; }
  .error-banner.show { display:block; }

  /* Toast */
  .toast { position:fixed;bottom:28px;left:50%;transform:translateX(-50%) translateY(80px);background:var(--surface);border:1px solid var(--border);border-radius:100px;padding:12px 26px;font-weight:500;font-size:0.87rem;transition:transform .3s cubic-bezier(.34,1.56,.64,1);z-index:400;white-space:nowrap; }
  .toast.show { transform:translateX(-50%) translateY(0); }
  .toast.success { border-color:var(--accent3);color:var(--accent3); }
  .toast.error { border-color:var(--accent2);color:var(--accent2); }
</style>
</head>
<body>
<div class="app">

<header>
  <div class="logo">Study<span>Mind</span></div>
  <div class="status-pill"><div class="status-dot"></div>IA activa</div>
</header>

<main>

  <!-- HERO -->
  <div class="hero">
    <div class="hero-tag">✦ Powered by Claude AI</div>
    <h1>Convierte tus apuntes en<br><span class="hl">juegos de estudio</span></h1>
    <p class="hero-sub">Sube cualquier documento de texto y la IA generará minijuegos visuales adaptados a tu asignatura.</p>
  </div>

  <!-- UPLOAD -->
  <div id="uploadSection">
    <div class="drop-zone" id="dropZone">
      <span class="drop-icon">📄</span>
      <div class="drop-title">Arrastra tus apuntes aquí</div>
      <div class="drop-sub">Archivos .txt o .md · Máximo 5 MB</div>
      <button class="drop-btn" onclick="document.getElementById('fileInput').click()">Seleccionar archivo</button>
      <input type="file" id="fileInput" accept=".txt,.md,.text">
    </div>

    <div class="file-preview" id="filePreview">
      <div class="file-icon-box">📝</div>
      <div class="file-info">
        <div class="file-name" id="fileName">—</div>
        <div class="file-size" id="fileSize">—</div>
      </div>
      <button class="remove-file" id="removeFile">✕</button>
    </div>

    <div class="error-banner" id="errorBanner"></div>

    <button class="analyze-btn" id="analyzeBtn">✦ Analizar y generar minijuegos</button>
  </div>

  <!-- LOADING -->
  <div class="loading-section" id="loadingSection">
    <div class="loader-ring"></div>
    <div class="loading-text">Analizando tu documento…</div>
    <div class="loading-sub">La IA está leyendo el contenido y generando actividades personalizadas</div>
    <div class="loading-steps">
      <div class="step-chip" id="s1">📖 Leyendo</div>
      <div class="step-chip" id="s2">🧠 Extrayendo ideas</div>
      <div class="step-chip" id="s3">🎮 Creando juegos</div>
      <div class="step-chip" id="s4">✓ Listo</div>
    </div>
  </div>

  <!-- RESULTS -->
  <div class="results-section" id="resultsSection">

    <div class="summary-card">
      <div class="subject-badge" id="subjectBadge"></div>
      <div class="summary-title" id="summaryTitle"></div>
      <div class="summary-desc" id="summaryDesc"></div>
      <div class="summary-grid">
        <div class="summary-block">
          <h4>Ideas principales</h4>
          <div id="ideasContainer"></div>
        </div>
        <div class="summary-block">
          <h4>Puntos clave</h4>
          <div id="keyPointsContainer"></div>
        </div>
      </div>
    </div>

    <div class="games-header">
      <div class="games-title">Minijuegos generados</div>
      <div class="games-count" id="gamesCount"></div>
    </div>
    <div class="games-grid" id="gamesGrid"></div>

    <div style="text-align:center;margin-top:40px;">
      <button class="reset-btn" id="newDocBtn" style="padding:12px 28px;font-size:0.9rem;">← Analizar otro documento</button>
    </div>
  </div>

</main>
</div>

<!-- GAME MODAL -->
<div class="game-modal" id="gameModal">
  <div class="gm-inner">
    <div class="gm-header">
      <div>
        <div class="gm-title" id="gmTitle"></div>
        <div class="gm-sub" id="gmSub"></div>
      </div>
      <button class="close-btn" onclick="closeModal()">✕</button>
    </div>
    <div class="gm-body" id="gmBody"></div>
  </div>
</div>

<div class="toast" id="toast"></div>

<script>
// ══════════════════════════════════════════════════════
// STATE
// ══════════════════════════════════════════════════════
let currentFile = null;
let analysisData = null;
let activeGames  = [];

// ══════════════════════════════════════════════════════
// FILE HANDLING
// ══════════════════════════════════════════════════════
const dropZone   = document.getElementById('dropZone');
const fileInput  = document.getElementById('fileInput');
const filePreview= document.getElementById('filePreview');
const analyzeBtn = document.getElementById('analyzeBtn');
const errorBanner= document.getElementById('errorBanner');

dropZone.addEventListener('dragover', e => { e.preventDefault(); dropZone.classList.add('dragover'); });
dropZone.addEventListener('dragleave', () => dropZone.classList.remove('dragover'));
dropZone.addEventListener('drop', e => {
  e.preventDefault(); dropZone.classList.remove('dragover');
  const f = e.dataTransfer.files[0];
  if (f) handleFile(f);
});
fileInput.addEventListener('change', e => { if (e.target.files[0]) handleFile(e.target.files[0]); });
document.getElementById('removeFile').addEventListener('click', clearFile);
document.getElementById('newDocBtn').addEventListener('click', () => {
  clearFile();
  document.getElementById('resultsSection').classList.remove('show');
  document.getElementById('uploadSection').style.display = '';
  window.scrollTo({ top: 0, behavior: 'smooth' });
});

function handleFile(f) {
  const allowed = ['.txt','.md','.text'];
  const ext = f.name.slice(f.name.lastIndexOf('.')).toLowerCase();
  if (!allowed.includes(ext) && f.type !== 'text/plain') {
    showError('Solo se admiten archivos .txt o .md'); return;
  }
  if (f.size > 5 * 1024 * 1024) { showError('El archivo supera los 5 MB.'); return; }
  currentFile = f;
  document.getElementById('fileName').textContent = f.name;
  document.getElementById('fileSize').textContent = fmtSize(f.size);
  filePreview.classList.add('show');
  analyzeBtn.classList.add('show');
  hideError();
}

function clearFile() {
  currentFile = null; fileInput.value = '';
  filePreview.classList.remove('show');
  analyzeBtn.classList.remove('show');
  hideError();
}

function fmtSize(b) {
  if (b < 1024) return b + ' B';
  if (b < 1024*1024) return (b/1024).toFixed(1) + ' KB';
  return (b/1024/1024).toFixed(1) + ' MB';
}

function showError(msg) { errorBanner.textContent = '⚠ ' + msg; errorBanner.classList.add('show'); }
function hideError() { errorBanner.classList.remove('show'); }

// ══════════════════════════════════════════════════════
// ANALYZE — calls our own Express backend
// ══════════════════════════════════════════════════════
analyzeBtn.addEventListener('click', startAnalysis);

async function startAnalysis() {
  if (!currentFile) return;
  hideError();
  analyzeBtn.disabled = true;
  document.getElementById('uploadSection').style.display = 'none';
  document.getElementById('loadingSection').classList.add('show');
  animateSteps();

  try {
    const fd = new FormData();
    fd.append('document', currentFile);

    const res = await fetch('/api/analyze', { method: 'POST', body: fd });
    const json = await res.json();

    if (!res.ok || !json.success) {
      throw new Error(json.error || `Error ${res.status}`);
    }

    analysisData = json.data;
    renderResults(json.data);

  } catch (err) {
    console.error(err);
    document.getElementById('uploadSection').style.display = '';
    showError(err.message || 'Error desconocido. Inténtalo de nuevo.');
    showToast('Error al analizar', 'error');
  } finally {
    document.getElementById('loadingSection').classList.remove('show');
    analyzeBtn.disabled = false;
  }
}

function animateSteps() {
  const ids = ['s1','s2','s3','s4'];
  ids.forEach(id => document.getElementById(id).className = 'step-chip');
  ids.forEach((id, i) => {
    setTimeout(() => {
      if (i > 0) document.getElementById(ids[i-1]).className = 'step-chip done';
      document.getElementById(id).className = 'step-chip active';
    }, i * 1800);
  });
}

// ══════════════════════════════════════════════════════
// RENDER RESULTS
// ══════════════════════════════════════════════════════
function renderResults(data) {
  const color = data.subjectColor || '#7c5cfc';

  // Badge
  const badge = document.getElementById('subjectBadge');
  badge.textContent = `${data.subjectEmoji} ${data.subject}`;
  badge.style.background = hex2rgba(color, 0.11);
  badge.style.border = `1px solid ${hex2rgba(color, 0.35)}`;
  badge.style.color = color;

  document.getElementById('summaryTitle').textContent = data.title;
  document.getElementById('summaryDesc').textContent  = data.description;

  document.getElementById('ideasContainer').innerHTML =
    (data.mainIdeas||[]).map(i => `<span class="idea-tag">${i}</span>`).join('');

  document.getElementById('keyPointsContainer').innerHTML =
    (data.keyPoints||[]).map(p => `<div class="key-point">${p}</div>`).join('');

  // Games
  activeGames = data.games || [];
  const grid = document.getElementById('gamesGrid');
  grid.innerHTML = '';
  document.getElementById('gamesCount').textContent = `${activeGames.length} actividades`;

  activeGames.forEach((g, idx) => {
    const card = document.createElement('div');
    card.className = 'game-card';
    card.style.setProperty('--gc', color);
    card.innerHTML = `
      <span class="game-emoji">${g.emoji}</span>
      <div class="game-name">${g.name}</div>
      <div class="game-desc">${g.description}</div>
      <span class="game-tag">${typeLabel(g.type)}</span>
      <button class="play-btn" onclick="openGame(${idx})">▶ Jugar ahora</button>
    `;
    grid.appendChild(card);
  });

  document.getElementById('resultsSection').classList.add('show');
  document.getElementById('resultsSection').scrollIntoView({ behavior: 'smooth' });
}

function typeLabel(t) {
  return { flashcards:'Tarjetas', ordering:'Ordenar', fill_blank:'Rellenar huecos', word_sort:'Ordenar palabras', memory:'Memoria', concept_map:'Mapa conceptual' }[t] || t;
}

// ══════════════════════════════════════════════════════
// MODAL
// ══════════════════════════════════════════════════════
function openGame(idx) {
  const g = activeGames[idx];
  if (!g) return;
  document.getElementById('gmTitle').textContent = `${g.emoji} ${g.name}`;
  document.getElementById('gmSub').textContent   = g.description;
  const body = document.getElementById('gmBody');
  body.innerHTML = '';
  const renderers = { flashcards:renderFlashcards, ordering:renderOrdering, fill_blank:renderFillBlank, word_sort:renderWordSort, memory:renderMemory, concept_map:renderConceptMap };
  (renderers[g.type] || (() => body.innerHTML = '<p style="color:var(--muted)">Juego no disponible.</p>'))(body, g.data);
  document.getElementById('gameModal').classList.add('show');
}

function closeModal() { document.getElementById('gameModal').classList.remove('show'); }
document.getElementById('gameModal').addEventListener('click', e => { if (e.target === document.getElementById('gameModal')) closeModal(); });

// ══════════════════════════════════════════════════════
// GAME: FLASHCARDS
// ══════════════════════════════════════════════════════
function renderFlashcards(el, data) {
  const cards = data.cards || [];
  const state = { idx: 0, seen: new Set() };

  el.innerHTML = `
    <div class="progress-bar"><div class="progress-fill" id="fc-prog"></div></div>
    <div class="flashcard-wrap">
      <div class="flashcard" id="fc-card">
        <div class="card-face card-front">
          <div class="card-label">Concepto</div>
          <div class="card-content" id="fc-front"></div>
          <div class="card-hint">Haz clic para ver la respuesta</div>
        </div>
        <div class="card-face card-back">
          <div class="card-label">Definición / Respuesta</div>
          <div class="card-content" id="fc-back"></div>
        </div>
      </div>
    </div>
    <div class="card-nav" style="margin-bottom:20px">
      <button class="nav-btn" id="fc-prev" onclick="fcNav(-1)">← Anterior</button>
      <span class="card-counter" id="fc-cnt"></span>
      <button class="nav-btn" id="fc-next" onclick="fcNav(1)">Siguiente →</button>
    </div>
    <div class="score-row"><span class="score-label">Cartas vistas</span><span class="score-val" id="fc-seen">0 / ${cards.length}</span></div>
  `;

  function update() {
    const c = cards[state.idx];
    document.getElementById('fc-front').textContent = c.front;
    document.getElementById('fc-back').textContent  = c.back;
    document.getElementById('fc-cnt').textContent   = `${state.idx+1} / ${cards.length}`;
    document.getElementById('fc-prog').style.width  = `${((state.idx+1)/cards.length)*100}%`;
    document.getElementById('fc-prev').disabled = state.idx === 0;
    document.getElementById('fc-next').disabled = state.idx === cards.length-1;
    document.getElementById('fc-card').classList.remove('flipped');
  }

  document.getElementById('fc-card').onclick = () => {
    const card = document.getElementById('fc-card');
    card.classList.toggle('flipped');
    if (card.classList.contains('flipped')) {
      state.seen.add(state.idx);
      document.getElementById('fc-seen').textContent = `${state.seen.size} / ${cards.length}`;
    }
  };

  window.fcNav = dir => { state.idx = Math.max(0, Math.min(cards.length-1, state.idx+dir)); update(); };
  update();
}

// ══════════════════════════════════════════════════════
// GAME: ORDERING
// ══════════════════════════════════════════════════════
function renderOrdering(el, data) {
  const items = [...(data.items||[])].sort(() => Math.random()-0.5);

  el.innerHTML = `
    <p style="color:var(--muted);font-size:0.85rem;margin-bottom:18px;">Arrastra los elementos para ponerlos en el orden correcto (1 = primero).</p>
    <ul class="timeline-list" id="ord-list"></ul>
    <div class="btn-row">
      <button class="check-btn" onclick="checkOrder()">Comprobar</button>
      <button class="reset-btn" onclick="renderOrdering(document.getElementById('gmBody'),${JSON.stringify(data).replace(/</g,'\\u003c')})">Reiniciar</button>
    </div>
    <div class="feedback-msg" id="ord-fb"></div>
  `;

  const list = document.getElementById('ord-list');
  items.forEach(item => {
    const li = document.createElement('li');
    li.className = 'timeline-item';
    li.dataset.order = item.order;
    li.draggable = true;
    li.innerHTML = `<span class="drag-handle">⠿</span><div class="timeline-num">?</div><div style="flex:1;font-size:.9rem">${item.text}</div>`;
    list.appendChild(li);
  });

  renumberOrd();
  let dragged = null;
  list.addEventListener('dragstart', e => { dragged = e.target.closest('.timeline-item'); dragged.classList.add('dragging'); });
  list.addEventListener('dragend',   () => { dragged?.classList.remove('dragging'); dragged = null; });
  list.addEventListener('dragover',  e => {
    e.preventDefault();
    const after = [...list.querySelectorAll('.timeline-item:not(.dragging)')].reduce((acc, ch) => {
      const box = ch.getBoundingClientRect();
      const off = e.clientY - box.top - box.height/2;
      return off < 0 && off > acc.off ? { off, el: ch } : acc;
    }, { off: -Infinity, el: null }).el;
    after ? list.insertBefore(dragged, after) : list.appendChild(dragged);
    renumberOrd();
  });

  function renumberOrd() {
    [...list.children].forEach((li,i) => li.querySelector('.timeline-num').textContent = i+1);
  }

  window.checkOrder = () => {
    const isOk = [...list.children].every((li,i) => parseInt(li.dataset.order) === i+1);
    [...list.children].forEach((li,i) => {
      li.classList.remove('correct','wrong');
      li.classList.add(parseInt(li.dataset.order)===i+1 ? 'correct' : 'wrong');
    });
    const fb = document.getElementById('ord-fb');
    fb.className = `feedback-msg show ${isOk?'correct':'wrong'}`;
    fb.textContent = isOk ? '🎉 ¡Perfecto! Has ordenado todo correctamente.' : '❌ Algunos elementos están en posición incorrecta. ¡Sigue intentándolo!';
  };
}

// ══════════════════════════════════════════════════════
// GAME: FILL BLANK
// ══════════════════════════════════════════════════════
function renderFillBlank(el, data) {
  const exs = data.exercises || [];
  let cur = 0, score = 0;

  function show() {
    const ex = exs[cur];
    const [before, after] = ex.sentence.split('___BLANK___');
    el.innerHTML = `
      <div class="progress-bar"><div class="progress-fill" style="width:${(cur/exs.length)*100}%"></div></div>
      <div class="score-row"><span class="score-label">Puntuación</span><span class="score-val">${score} / ${exs.length}</span></div>
      <div class="blank-sentence">${before}<input class="blank-input" id="bl-in" type="text" placeholder="…" autocomplete="off">${after||''}</div>
      <p style="color:var(--muted);font-size:0.8rem;margin-bottom:18px;">💡 Pista: ${ex.hint}</p>
      <div class="btn-row">
        <button class="check-btn" id="bl-check">Comprobar</button>
        <button class="next-btn" id="bl-next" style="display:none">Siguiente →</button>
      </div>
      <div class="feedback-msg" id="bl-fb"></div>
    `;

    const input = document.getElementById('bl-in');
    const checkFn = () => {
      const val = input.value.trim().toLowerCase();
      const ok  = val === ex.answer.trim().toLowerCase();
      if (ok) score++;
      input.classList.add(ok ? 'correct' : 'wrong');
      input.disabled = true;
      document.getElementById('bl-check').style.display = 'none';
      const fb = document.getElementById('bl-fb');
      fb.className = `feedback-msg show ${ok?'correct':'wrong'}`;
      fb.textContent = ok ? '✅ ¡Correcto!' : `❌ La respuesta correcta es: "${ex.answer}"`;
      if (cur < exs.length-1) document.getElementById('bl-next').style.display = '';
      else {
        setTimeout(() => {
          el.innerHTML = `<div class="complete-screen"><div class="complete-icon">🎊</div><div class="complete-title">¡Completado!</div><div class="complete-sub">Resultado: ${score} de ${exs.length} correctas</div><button class="check-btn" onclick="renderFillBlank(document.getElementById('gmBody'),${JSON.stringify(data).replace(/</g,'\\u003c')})">Jugar de nuevo</button></div>`;
        }, 1500);
      }
    };

    document.getElementById('bl-check').onclick = checkFn;
    document.getElementById('bl-next').onclick   = () => { cur++; show(); };
    input.addEventListener('keydown', e => { if (e.key==='Enter') checkFn(); });
    input.focus();
  }

  show();
}

// ══════════════════════════════════════════════════════
// GAME: WORD SORT
// ══════════════════════════════════════════════════════
function renderWordSort(el, data) {
  const sentences = data.sentences || [];
  let cur = 0, score = 0;

  function show() {
    const s = sentences[cur];
    const shuffled = [...s.words].sort(() => Math.random()-0.5);
    let placed = [];

    el.innerHTML = `
      <div class="progress-bar"><div class="progress-fill" style="width:${(cur/sentences.length)*100}%"></div></div>
      <div class="score-row"><span class="score-label">Puntuación</span><span class="score-val">${score} / ${sentences.length}</span></div>
      <p style="color:var(--muted);font-size:0.85rem;margin-bottom:14px;">Toca las palabras en el orden correcto para formar la frase:</p>
      <div class="sentence-builder" id="ws-build"></div>
      <div class="word-bank" id="ws-bank"></div>
      <div class="btn-row" style="margin-top:18px">
        <button class="check-btn" id="ws-check">Comprobar</button>
        <button class="reset-btn" onclick="resetWS()">Limpiar</button>
      </div>
      <div class="feedback-msg" id="ws-fb"></div>
    `;

    const bank  = document.getElementById('ws-bank');
    const build = document.getElementById('ws-build');
    const chips = [];

    shuffled.forEach((w, i) => {
      const ch = document.createElement('div');
      ch.className = 'word-chip';
      ch.textContent = w;
      ch.dataset.i = i;
      ch.onclick = () => {
        if (ch.classList.contains('placed')) return;
        ch.classList.add('placed');
        placed.push({ word: w, chip: ch });
        const bc = document.createElement('div');
        bc.className = 'word-chip in-builder';
        bc.textContent = w;
        bc.onclick = () => {
          const pi = placed.findIndex(p => p.chip === ch);
          if (pi !== -1) placed.splice(pi, 1);
          ch.classList.remove('placed');
          build.removeChild(bc);
          if (!build.children.length) build.classList.remove('active');
        };
        build.appendChild(bc);
        build.classList.add('active');
      };
      bank.appendChild(ch);
      chips.push(ch);
    });

    window.resetWS = () => {
      placed = []; build.innerHTML = ''; build.classList.remove('active');
      chips.forEach(ch => ch.classList.remove('placed'));
      document.getElementById('ws-fb').className = 'feedback-msg';
    };

    document.getElementById('ws-check').onclick = () => {
      const formed = placed.map(p=>p.word).join(' ');
      const ok = formed.toLowerCase().trim() === s.correct.toLowerCase().trim();
      if (ok) score++;
      const fb = document.getElementById('ws-fb');
      fb.className = `feedback-msg show ${ok?'correct':'wrong'}`;
      fb.textContent = ok ? '✅ ¡Perfecto! Frase correcta.' : `❌ La frase correcta es: "${s.correct}"`;
      setTimeout(() => {
        cur++;
        if (cur < sentences.length) show();
        else el.innerHTML = `<div class="complete-screen"><div class="complete-icon">🎊</div><div class="complete-title">¡Completado!</div><div class="complete-sub">Resultado: ${score} de ${sentences.length}</div><button class="check-btn" onclick="renderWordSort(document.getElementById('gmBody'),${JSON.stringify(data).replace(/</g,'\\u003c')})">Jugar de nuevo</button></div>`;
      }, 1800);
    };
  }

  show();
}

// ══════════════════════════════════════════════════════
// GAME: MEMORY
// ══════════════════════════════════════════════════════
function renderMemory(el, data) {
  const pairs = data.pairs || [];
  const allCards = [];
  pairs.forEach((p,i) => {
    allCards.push({ id:i, text:p.term,       type:'term' });
    allCards.push({ id:i, text:p.definition, type:'def'  });
  });
  allCards.sort(() => Math.random()-0.5);

  let flipped=[], matched=[], moves=0, locked=false;

  el.innerHTML = `
    <div class="score-row"><span class="score-label">Movimientos</span><span class="score-val" id="mem-m">0</span></div>
    <div class="memory-grid" id="mem-grid"></div>
    <div class="feedback-msg" id="mem-fb"></div>
  `;

  const grid = document.getElementById('mem-grid');
  allCards.forEach((card,i) => {
    const div = document.createElement('div');
    div.className = 'mem-card';
    div.innerHTML = `<div class="mem-card-inner"><div class="mem-face mem-back">🧠</div><div class="mem-face mem-front">${card.text}</div></div>`;
    div.onclick = () => flip(div, card, i);
    grid.appendChild(div);
  });

  function flip(el, card, i) {
    if (locked || flipped.some(f=>f.i===i) || matched.includes(card.id)) return;
    el.classList.add('flipped');
    flipped.push({ el, card, i });

    if (flipped.length === 2) {
      locked = true; moves++;
      document.getElementById('mem-m').textContent = moves;
      const [a,b] = flipped;
      if (a.card.id === b.card.id && a.card.type !== b.card.type) {
        matched.push(a.card.id);
        a.el.classList.add('matched'); b.el.classList.add('matched');
        a.el.querySelectorAll('.mem-face').forEach(f => { f.style.borderColor='var(--accent3)'; });
        b.el.querySelectorAll('.mem-face').forEach(f => { f.style.borderColor='var(--accent3)'; });
        flipped = []; locked = false;
        if (matched.length === pairs.length) {
          const fb = document.getElementById('mem-fb');
          fb.className = 'feedback-msg show correct';
          fb.textContent = `🎉 ¡Has completado el juego de memoria en ${moves} movimientos!`;
        }
      } else {
        setTimeout(() => {
          a.el.classList.remove('flipped'); b.el.classList.remove('flipped');
          flipped = []; locked = false;
        }, 900);
      }
    }
  }
}

// ══════════════════════════════════════════════════════
// GAME: CONCEPT MAP
// ══════════════════════════════════════════════════════
function renderConceptMap(el, data) {
  const nodes = data.nodes || [];
  const edges = data.edges || [];

  el.innerHTML = `
    <p style="color:var(--muted);font-size:0.82rem;margin-bottom:14px;">Mapa conceptual interactivo. Haz clic en los nodos para ver sus conexiones.</p>
    <div class="concept-map" id="cmap"></div>
    <div id="node-info" style="margin-top:12px;padding:12px 18px;border-radius:10px;background:var(--card);border:1px solid var(--border);font-size:0.85rem;color:var(--muted);display:none;"></div>
  `;

  const mapEl = document.getElementById('cmap');
  const svg = document.createElementNS('http://www.w3.org/2000/svg','svg');
  svg.setAttribute('viewBox','0 0 100 100');
  svg.setAttribute('preserveAspectRatio','xMidYMid meet');

  // Defs for arrow
  const defs = document.createElementNS('http://www.w3.org/2000/svg','defs');
  const marker = document.createElementNS('http://www.w3.org/2000/svg','marker');
  marker.setAttribute('id','arrow'); marker.setAttribute('markerWidth','6'); marker.setAttribute('markerHeight','6');
  marker.setAttribute('refX','5'); marker.setAttribute('refY','3'); marker.setAttribute('orient','auto');
  const poly = document.createElementNS('http://www.w3.org/2000/svg','polygon');
  poly.setAttribute('points','0 0, 6 3, 0 6'); poly.setAttribute('fill','#3a3a5a');
  marker.appendChild(poly); defs.appendChild(marker); svg.appendChild(defs);

  // Edges
  edges.forEach(e => {
    const fn = nodes.find(n=>n.id===e.from), tn = nodes.find(n=>n.id===e.to);
    if (!fn||!tn) return;
    const line = document.createElementNS('http://www.w3.org/2000/svg','line');
    line.setAttribute('x1',fn.x); line.setAttribute('y1',fn.y);
    line.setAttribute('x2',tn.x); line.setAttribute('y2',tn.y);
    line.setAttribute('stroke','#2a2a3d'); line.setAttribute('stroke-width','0.7');
    line.setAttribute('marker-end','url(#arrow)');
    svg.appendChild(line);

    if (e.label) {
      const tx = document.createElementNS('http://www.w3.org/2000/svg','text');
      tx.setAttribute('x',(fn.x+tn.x)/2); tx.setAttribute('y',(fn.y+tn.y)/2-1);
      tx.setAttribute('text-anchor','middle'); tx.setAttribute('font-size','2.6');
      tx.setAttribute('fill','#5a5a7a'); tx.textContent = e.label;
      svg.appendChild(tx);
    }
  });

  // Nodes
  nodes.forEach(n => {
    const color = n.color || '#7c5cfc';
    const g = document.createElementNS('http://www.w3.org/2000/svg','g');
    g.style.cursor = 'pointer';

    const circle = document.createElementNS('http://www.w3.org/2000/svg','circle');
    circle.setAttribute('cx',n.x); circle.setAttribute('cy',n.y); circle.setAttribute('r','7.5');
    circle.setAttribute('fill', hex2rgba(color,0.18));
    circle.setAttribute('stroke',color); circle.setAttribute('stroke-width','1');
    circle.style.transition = 'all .2s';

    const txt = document.createElementNS('http://www.w3.org/2000/svg','text');
    txt.setAttribute('x',n.x); txt.setAttribute('y',n.y+1);
    txt.setAttribute('text-anchor','middle'); txt.setAttribute('dominant-baseline','middle');
    txt.setAttribute('font-size','3.2'); txt.setAttribute('fill','#f0eeff'); txt.setAttribute('font-weight','600');
    txt.textContent = n.label.length > 13 ? n.label.slice(0,12)+'…' : n.label;

    g.appendChild(circle); g.appendChild(txt);
    g.onclick = () => {
      // Highlight connected edges
      circle.setAttribute('fill', hex2rgba(color, 0.45));
      setTimeout(() => circle.setAttribute('fill', hex2rgba(color, 0.18)), 1000);
      const conn = edges.filter(e=>e.from===n.id||e.to===n.id);
      const info = document.getElementById('node-info');
      if (conn.length) {
        info.style.display = 'block';
        info.innerHTML = `<strong style="color:${color}">${n.label}</strong> se conecta con: ` +
          conn.map(e => {
            const other = nodes.find(nd => nd.id === (e.from===n.id ? e.to : e.from));
            return `<span style="color:var(--text)">${other?.label||'?'}</span>${e.label?' <em style="color:var(--muted)">('+e.label+')</em>':''}`;
          }).join(', ');
      }
    };
    svg.appendChild(g);
  });

  mapEl.appendChild(svg);
}

// ══════════════════════════════════════════════════════
// UTILITIES
// ══════════════════════════════════════════════════════
function hex2rgba(hex, a) {
  const r = /^#?([a-f\d]{2})([a-f\d]{2})([a-f\d]{2})$/i.exec(hex);
  if (!r) return `rgba(124,92,252,${a})`;
  return `rgba(${parseInt(r[1],16)},${parseInt(r[2],16)},${parseInt(r[3],16)},${a})`;
}

function showToast(msg, type='success') {
  const t = document.getElementById('toast');
  t.textContent = msg; t.className = `toast show ${type}`;
  setTimeout(() => t.classList.remove('show'), 3000);
}
</script>
</body>
</html>
